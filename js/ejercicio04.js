let base = 8;
let altura = 5;

const area = (b, a) => b * a;
const perimetro = (b, a) => 2 * b + 2 * a;

console.log(`El area del rectangulo es: ${area(base, altura)} cm² y su perimetro es: ${perimetro(base, altura)} cm²`);